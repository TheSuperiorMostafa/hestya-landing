{
  "name": "hestya-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "rm -rf .next out && next build",
    "start": "next start",
    "export": "next build && next export",
    "build:cf": "rm -rf .next out && npm run build && echo 'Build completed successfully for Cloudflare Pages'"
  },
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.4.12",
    "postcss": "^8.4.16",
    "tailwindcss": "^3.2.4",
    "typescript": "^5.1.6"
  }
}
